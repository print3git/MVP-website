name: Matrix labels
on: push
jobs:
  build:
    strategy:
      matrix:
        runner:
          - [self-hosted, linux, x64, mvp-gh-runner]
    runs-on: ${{ matrix.runner }}
    steps:
      - run: echo ok
