name: bad
on: push
permissions:
  actions: read
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      actions: read
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            const core = {};
            try {
              core.notice('hi');
            } catch (error) {
              core.setFailed(error.message);
            }
