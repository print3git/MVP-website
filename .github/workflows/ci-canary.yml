name: CI Canary
on:
  push:
    branches: ["**"]
  pull_request:
jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - run: echo "Canary CI check running"
