name: pnpm guard

on:
  workflow_call:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - run: corepack enable
      - uses: pnpm/action-setup@v4.0.0
        with:
          version: 8.15.8
      - run: pnpm install --no-frozen-lockfile
