name: Prod Env Guard

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      reviewers:
        - octocat
    steps:
      - run: echo "Deploy placeholder"
