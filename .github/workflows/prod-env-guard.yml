name: Prod Env Guard

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on:
      - [self-hosted, linux, aws]
      - [self-hosted, linux, x64, ci-small]
    environment:
      name: production
      reviewers:
        - octocat
    steps:
      - run: echo "Deploy placeholder"
