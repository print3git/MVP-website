name: LFS guard / lfs-guard
on: [pull_request, push]
jobs:
  lfs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5.0.0
        with: { fetch-depth: 0 }
      - name: Audit LFS pointers
        run: ci/lfs/audit.sh
        shell: bash
