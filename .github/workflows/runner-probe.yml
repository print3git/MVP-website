name: Runner probe

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: [self-hosted, linux, aws-ephemeral]
    steps:
      - name: Print system info
        run: |
          uname -a
          cat /etc/os-release
          echo "Runner context: ${{ toJSON(runner) }}"
