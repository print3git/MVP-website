name: CI AWS Sanity
on:
  push:
    branches: ["**"]
  pull_request:
jobs:
  aws-sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - run: echo "AWS sanity CI check running"
