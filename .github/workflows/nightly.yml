name: Nightly CI

env:
  HUSKY: 0


on:
  schedule:
    - cron: '0 3 * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    timeout-minutes: 15
    uses: ./.github/workflows/node.yml
