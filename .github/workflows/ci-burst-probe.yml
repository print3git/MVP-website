name: CI Burst Probe
on:
  push:
    branches: ["**"]
  pull_request:
jobs:
  burst-probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - run: echo "Burst probe CI check running"
