name: Queue Probe

on:
  pull_request:
  workflow_dispatch:


permissions: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo "hello $GITHUB_RUN_ID"
    env:
      HUSKY: "0"
