#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npm audit --audit-level=high
npx lint-staged
grep -R --line-number 'npm ci' .github/workflows Dockerfile && { echo '❌  Use npm install, not npm ci'; exit 1; } || true
